"""
Store the entirity of the sonnets as a string. Add a function in ebooks.py
that iterates thru it and splits it into an array by linebreaks ('\n'). Then use
this array in the place of the tweets array from the original program. Yes!

tweets.split('\n')

Also, filter out lines that start with:
XC LX II IV VI CX L. C. CL LI. LII
"""
